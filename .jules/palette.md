## 2024-05-24 - Missing ARIA Labels on Icon Buttons
**Learning:** Found multiple instances of icon-only buttons missing `aria-label` attributes across different game components (e.g., `closeBtn`, `iconBtn`). This is a recurring pattern that impacts screen reader users.
**Action:** Ensure all icon-only buttons have descriptive `aria-label` attributes to support accessibility.
